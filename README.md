# Word-frequecy-analysis
This project implements a simple program that analyzes the frequency of words in a text file.

The program reads a text file, normalizes the content, counts how many times each word appears, and displays the **10 most frequent words**.